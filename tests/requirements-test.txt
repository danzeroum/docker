# Dependências de teste — não entram na imagem de produção
fastapi==0.115.5
httpx==0.27.2
uvicorn[standard]==0.32.1
pytest==8.3.5
pytest-asyncio==0.24.0
respx==0.21.1          # mock de httpx (intercepta chamadas ao socket-proxy)
psutil==6.1.0
